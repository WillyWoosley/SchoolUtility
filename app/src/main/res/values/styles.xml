<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Real.Deal">
        <!-- Customize your theme here. -->
        <!--
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        -->
        <item name="android:textColorSecondary">@color/authui_inputTextColorDark</item>
        <item name="android:textColorPrimary">@color/authui_inputTextColorDark</item>
    </style>

    <style name="Real.Deal" parent="Theme.AppCompat.NoActionBar" >
        <item name="colorAccent">@color/accent_material_dark_1</item>
        <item name="android:colorBackground">@color/background_material_dark</item>
        <item name="android:colorForeground">@color/foreground_material_dark</item>
    </style>

</resources>
